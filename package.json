{
  "name": "portalcamara",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "check": "node --check worker.js && node --check auth.js",
    "test": "node --test tests/*.test.mjs",
    "auth:generate": "node tools/generate-auth-users.mjs"
  },
  "devDependencies": {
    "wrangler": "latest"
  }
}
